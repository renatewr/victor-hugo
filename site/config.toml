baseurl = "/"
title = "Cecilie Bibow"
recentposts = "Siste historier"
languageCode = "en-us"
# Site language. Available translations in the theme's `/i18n` directory.
defaultContentLanguage = "en"
# Enable comments by entering your Disqus shortname
#disqusShortname = "devcows"
# Enable Google Analytics by entering your tracking code
googleAnalytics = ""

# Define the number of posts per page
paginate = 100

[menu]

[[menu.main]]
    name = "For bedrifter"
    url  = "/bedrift/"
    weight = 1

[[menu.main]]
    name = "Om Cecilie"
    url  = "/about/"
    weight = 4

[[menu.main]]
    name = "Kontakt"
    url  = "/contact/"
    weight = 3


[params]
    viewMorePostLink = "/blog/"
    author = "Cecilie"
    defaultKeywords = ["velvaere", "bedriftsmassasje","muskelsmerter","hudpleie", "smertelindring", "massasje", "bedriftshelsetjeneste", "massasje på jobb"]
    defaultDescription = "Cecilie Bibow tilbyr skreddersydd massasje og smertebehandling til ansatte i bedrifter."

    # Google Maps API key (if not set will default to not passing a key.)
    googleMapsApiKey = "AIzaSyAx6SfHdU5oSj4Co6Mjp5bPZGZZtrBuaw4"

    # Style options: default (light-blue), blue, green, marsala, pink, red, turquoise, violet
    style = "marsala"

    # Since this template is static, the contact form uses www.formspree.io as a
    # proxy. The form makes a POST request to their servers to send the actual
    # email. Visitors can send up to a 1000 emails each month for free.
    #
    # What you need to do for the setup?
    #
    # - set your email address under 'email' below
    # - upload the generated site to your server
    # - send a dummy email yourself to confirm your account
    # - click the confirm link in the email from www.formspree.io
    # - you're done. Happy mailing!
    #
    # Enable the contact form by entering your Formspree.io email
    email = "cecilie@oslobliny.no"

    about_us = "<p>Cecilie Bibow</p></br>"
    copyright = "Copyright (c) 2017, Cecilie Bibow; all rights reserved."

    # Format dates with Go's time formatting
    date_format = "January 2, 2006"

    logo = "img/logo.png"
    address = """<p><strong>Cecilie Bibow</strong></p>"""
     latitude = "59.9115833"
     longitude = "10.7412500"

[Permalinks]
    blog = "/blog/:filename/"
    post = "/post/:year/:month/:day/:filename/"

# Enable and disable widgets for the right sidebar
[params.widgets]
    categories = false
    tags = false
    search = false

[params.carousel]
    enable = false
    # All carousel items are defined in their own files. You can find example items
    # at 'exampleSite/data/carousel'.
    # For more informtion take a look at the README.

[params.offers]
    enable = true
    # All features are defined in their own files. You can find example items


[params.features]
    enable = true
    # All features are defined in their own files. You can find example items
    # at 'exampleSite/data/features'.
    # For more informtion take a look at the README.

[params.testimonials]
    enable = true
    # All testimonials are defined in their own files. You can find example items
    # at 'exampleSite/data/testimonials'.
    # For more informtion take a look at the README.

[params.clients]
    enable = false
    # All clients are defined in their own files. You can find example items
    # at 'exampleSite/data/clients'.
    # For more informtion take a look at the README.
    title = "Samarbeid"
    subtitle = ""

[params.recent_posts]
    enable = true
    title = "Vi tilbyr"
    subtitle = ""

[params.see_more]
    enable = true
     icon = "fa fa-heart-o"
     title = "Vil du vite mer om smerteeksperten?"
     subtitle = "Bedriftsmassasje og smertebehandling"
     link_url = "/categories/smerteeksperten/"
     link_text = "Gå til smerteeksperten"
